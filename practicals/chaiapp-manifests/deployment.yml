apiVersion: apps/v1
kind: Deployment
metadata:
  name: chai-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: chai-app
  template:
    metadata:
      labels:
        app: chai-app
    spec:
      containers:
      - name: chai-app
        image: shubhamm18/chaiapp-devops:v1.0.0   # Updated Docker image
        ports:
        - containerPort: 80
        env:
        - name: VITE_GEMINI_API
          valueFrom:
            secretKeyRef:
              name: chai-app-secret    # secret name
              key: VITE_GEMINI_API     # define in secret.yml of same folder
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "200m"
